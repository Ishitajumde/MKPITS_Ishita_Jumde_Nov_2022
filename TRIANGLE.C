#include<stdio.h>
#include<conio.h>
void main()
{
int an1,an2,an3,p,sum;
clrscr();
printf("enter the value for three angle");
scanf("%d %d",&an1,an2,an3);
sum=an1+an2+an3;
  if(sum=180 && an1!=0 && an2!=0 && an3!=0)
{
      printf("traingle is valid");
}
if
{
   p=an1+an2+an3;
}
printf("parameter of triangl=%d",p);
getch();
}


